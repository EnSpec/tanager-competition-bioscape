# Python dependencies for the Tanager trait-mapping scripts.
# Install into a project-local venv (.venv/), NOT the shared base conda
# env - rasterio/pyproj pull in numpy 2.x, which breaks version pins for
# scipy/numba/gensim/contourpy in the shared /opt/anaconda3 base env used
# by other projects. See README's Environment section.
#
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

h5py==3.16.0
fsspec==2026.7.0
requests==2.34.2
numpy==2.5.2
scipy==1.18.1
rasterio==1.5.1
pyproj==3.7.2
matplotlib==3.11.1
netCDF4==1.7.4
aiohttp
pandas==3.0.5
